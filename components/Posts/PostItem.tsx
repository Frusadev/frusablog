import { Post } from '@/objects/Post'
import React from 'react'

type PostProps = {
  postAppearence: "list" | "card"
  postObject: Post
}

const PostItem = (prosp: PostProps) => {
  return (
    <div>
      
      </div>
  )
}

export default PostItem
